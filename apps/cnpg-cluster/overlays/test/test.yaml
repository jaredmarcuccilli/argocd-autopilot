apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: cluster-test
spec:
  instances: 3
  storage:
    size: 1Gi
  bootstrap:
    initdb:
      database: keycloak
      owner: keycloak
      secret:
        name: bw-cnpg-cluster-test-secret
